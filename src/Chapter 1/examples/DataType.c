/* float can calculate a very very long numbers,
   but int cannot. */


#include<stdio.h>
#include<math.h>

int main()
{
    // double a = 11111111111111111;
    // int b = -9;
    // printf("%f\n", sqrt(b));
    printf("%d\n", 0);
    return 0;
}